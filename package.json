{
  "name": "automed",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "bunx --bun vite",
    "build": "bunx --bun vite build --outDir docs",
    "preview": "bunx --bun vite preview --outDir docs"
  },
  "devDependencies": {
    "vite": "^4.4.11"
  }
}
